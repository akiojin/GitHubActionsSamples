name: Trigger main or release branch

on:
  push:
    branches:
      - main
      - release/**

jobs:
  Example:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Pushed to the main or release branch."
